<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="zvhir.dev.design4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design4.NoActionBar">
        <activity android:name=".devices.Lights"></activity>
        <activity android:name=".devices.AirConditioner" />
        <activity android:name=".devices.WaterHeater" />
        <activity android:name=".rooms.MeetingRoom" />
        <activity android:name=".rooms.Kitchen" />
        <activity android:name=".rooms.Office" />
        <activity android:name=".rooms.LivingRoom" />
        <activity android:name=".Splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Design4.NoActionBar" />
    </application>

</manifest>