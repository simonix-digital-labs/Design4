<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="SelectionSwitch" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/green</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/light_grey
        </item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">@color/light_grey
        </item>
    </style>

    <style name="WhiteSwitch" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/white</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/white
        </item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">@color/light_grey
        </item>
    </style>


    <style name="YellowSwitch" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/yellow</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/white
        </item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">@color/light_grey
        </item>
    </style>


    <style name="GreyBlack" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/black</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">#4d4d4d
        </item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">#666666
        </item>
    </style>
</resources>

